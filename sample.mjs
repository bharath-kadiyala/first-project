export class StudentDetails {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
}

export class CarDetails {
  constructor(brand, color) {
    this.brand = brand;
    this.color = color;
  }
}
